# frozen_string_literal: true

RSpec.describe Cocoapods::Play do
  it "has a version number" do
    expect(Cocoapods::Play::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
